services:
  backend:
    build: ./backend
    restart: always
    ports: 
      - "${PORT}:${PORT}"
    environment:
      - PORT=${PORT}
      - DATABASE_URL=${DATABASE_URL}
    env_file:
      - .env
    volumes:
      - ./backend:/app
      - /app/node_modules
